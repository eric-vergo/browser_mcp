{
  "name": "browser-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "Single VSCode extension: real-DOM docs pane + co-located Playwright + hosted MCP for AI-driven documentation development.",
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "build": "npm -w extension run build",
    "typecheck": "npm -w extension run typecheck",
    "install:ext": "bash scripts/install-extension.sh",
    "package:ext": "npm -w extension run package"
  }
}
